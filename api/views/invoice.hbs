 // .skip(perPage * page)
                // .limit(perPage)
            

                // .then(result => {
                // let original =    result.map((results)=>{
                //      return{ 
                //          book_name : results.book_name,
                //          active_bool : results.active_bool,
                //          mrp_dollar : results.mrp_dollar,
                //          mrp_euro : results.mrp_euro,
                //          mrp_aus_dollar : results.mrp_aus_dollar,
                //          mrp_pound : results.mrp_pound,
                //          author_name : results.author_name,
                //          Isbn_no : results.Isbn_no,
                //          publisher : results.publisher,
                //          condition : results.condition,
                //          print_format : results.print_format,
                //          mrp_inr : results.mrp_inr,
                //          sale_price : results.sale_price,
                //          description : results.description,
                //          publication_year : results.publication_year,
                //          no_Of_pages : results.no_Of_pages,
                //          language : results.language,
                //          dimensions : results.dimensions,
                //          rate : results.rate,
                //          weight : results.weight,
                //          categories : results.categories,
                //          subcategory : results.subcategory,
                //          quantity : results.quantity,
                //          sku : results.sku,
                //          country_origin : results.country_origin,
                //           book_img : results.book_img,
                //          discount_per : results.discount_per,
                //          discount_rs : results.discount_rs,
                //          final_price : Math.floor(results.final_price),
                //          sale_disc_per : results.sale_disc_per,
                //          sale_disc_inr : results.sale_disc_inr,
                //          sale_rate : results.sale_rate
                //      }
                //     })
                //  let sorted =   original.sort(function(a,b){
                //         return a.final_price - b.final_price
                //     })
                //     console.log(sorted)
                   
                //     var pag = Math.ceil(totalBooks / perPage);
                //     if (pag > page) {
                //         res.status(200).json({
                //             success: true,
                //             books: sorted,
                //             totalBooks: totalBooks,
                //             pages: Math.ceil(totalBooks / perPage )
                //         });
                //     } else {
                //         res.status(200).json({
                //             success: false,
                //             books: [],
                //             totalBooks: 0,
                //             pages: 0
                //         });
                //     }
                    

                // })
                // .catch(error => {
                //     res.status(500).json({
                //         error: error
                //     });
                // });